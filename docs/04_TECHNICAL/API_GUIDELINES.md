# API Guidelines

> **Build once.**
>
> **Record once.**
>
> **Trust forever.**

---

## Purpose

This document defines the API design rules for ConstructIQ.

The API must be secure, predictable, consistent and easy to consume from the frontend.

---

## API Style

ConstructIQ v1.0 uses a REST API.

Base path:

```text
/api/v1